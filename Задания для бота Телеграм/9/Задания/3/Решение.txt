Сначала будем проверять сколько раз в строке встречается число. Для этого в ячейку F1 запишем формулу

=СЧЁТЕСЛИ($A1:$E1;A1)
и скопируем её во все ячейки диапазона F1:J16000. В ячейке К1 запишем формулу =СУММ(F1:J1). Если число в ячейке равно 5, то в ряду все числа встречаются один раз. Скопируем формулу во все ячейки диапазона К1:К16000. В ячейке L1 посчитаем удвоенную сумму максимального и минимального значений. Для этого в ячейку L1 запишем формулу
=ЕСЛИ(K1=5;2*(МАКС(A1:E1)+МИН(A1:E1));0)
и скопируем её во все ячейки диапазона L1:L16000. В ячейке M1 посчитаем сумму оставшихся чисел. Для этого в ячейку M1 запишем формулу
=ЕСЛИ(K1=5;(СУММ(A1:E1)-МАКС(A1:E1)-МИН(A1:E1));0)
и скопируем её во все ячейки диапазона M1:M16000. В ячейке N1 будем выписывать 1, если ряд подходит под условие задачи и 0 если не подходит. Для этого в ячейку N1 запишем формулу
=ЕСЛИ(K1=5;ЕСЛИ(L1<=M1;1;0);0)
и скопируем её во все ячейки диапазона N1:N16000. Окончательно, с помощью формулы =СУММ(N1:N16000) получим ответ  — 607.
 

Ответ: 607.